class Place {
  constructor(id, title, imageUri, address, lat, lng, dateVisited) {
    this.id = id;
    this.title = title;
    this.imageUri = imageUri;
    this.address = address;
    this.lat = lat;
    this.lng = lng;
    this.dateVisited = dateVisited;
  }
}

export default Place;
